# Development happens on Windows while everything is built in Linux
# containers. Without this git can store CRLF, which breaks shell scripts and
# entrypoints inside the container with a confusing "no such file or
# directory". Pin LF for all text files.
* text=auto eol=lf
*.png binary
*.jpg binary
